#!/usr/bin/env sh

if [ $(apt-cache pkgnames | grep -c "libstdc++-4.9-dev") -eq 0 ];
then
  sudo add-apt-repository ppa:ubuntu-toolchain-r/test -y &&
  sudo apt-get update -y &&
  sudo apt-get install libstdc++-4.9-dev -y
fi
